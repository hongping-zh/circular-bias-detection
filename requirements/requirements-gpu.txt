# CBD GPU 加速环境依赖
# 
# 安装方式:
# pip install -r requirements-gpu.txt

# PyTorch (GPU 版本)
# 根据您的 CUDA 版本选择:
# CUDA 11.8:
# torch>=2.0.0
# torchvision>=0.15.0
# torchaudio>=2.0.0
# 安装: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# CUDA 12.1:
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
# 安装: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Sentence Transformers
sentence-transformers>=2.2.0
transformers>=4.30.0

# CuPy (GPU 加速的 NumPy)
# 根据 CUDA 版本选择:
# cupy-cuda11x>=12.0.0  # CUDA 11.x
# cupy-cuda12x>=12.0.0  # CUDA 12.x

# 加速库
accelerate>=0.20.0

# GPU 监控
nvidia-ml-py3>=11.0.0
pynvml>=11.0.0

# 基础依赖（与 requirements.txt 一致）
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0
datasets>=2.14.0
tqdm>=4.65.0

# 可选：性能分析
# torch-tb-profiler>=0.4.0
# tensorboard>=2.13.0

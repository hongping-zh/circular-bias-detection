# 系统整合测试指南

## 🎯 测试目标

验证 circular-bias-detector 整合后，整个 MVP 系统是否正常工作。

---

## 📋 测试清单

- [ ] 后端服务器启动
- [ ] 后端 API 端点测试
- [ ] 前端应用启动
- [ ] 前端功能测试
- [ ] 端到端集成测试

---

## 🚀 第一步：启动后端

### 方法 1: 手动启动

```powershell
# 打开新的 PowerShell 窗口
cd C:\Users\14593\CascadeProjects\circular-bias-detection\backend

# 启动后端
python app.py
```

**预期输出**:
```
[OK] circular-bias-detector package loaded successfully
======================================================================
🚀 Starting Sleuth API Server with Zenodo Integration
======================================================================

Endpoints:
  GET  /health                - Health check
  GET  /api/info              - API information
  POST /api/detect            - Bias detection (custom data)
  POST /api/analyze-csv       - Gemini AI CSV analysis
  ...

Server running on: http://localhost:5000
======================================================================
```

**关键检查**:
- ✅ 看到 `[OK] circular-bias-detector package loaded successfully`
- ✅ 服务器运行在 Port 5000
- ✅ 没有错误信息

---

## 🧪 第二步：测试后端 API

### 自动测试脚本

在**新的 PowerShell 窗口**运行:

```powershell
cd C:\Users\14593\CascadeProjects\circular-bias-detection\backend
python test_api_endpoints.py
```

### 手动测试（可选）

```powershell
# Test 1: Health Check
curl http://localhost:5000/health

# 预期输出: {"status":"ok","service":"Sleuth Bias Detection API","version":"1.0.0"}

# Test 2: API Info
curl http://localhost:5000/api/info
```

### 测试结果评估

**成功标志**:
- ✅ [TEST 1] Health check: PASS
- ✅ [TEST 2] API info: PASS
- ✅ [TEST 3] Bias detection: PASS
- ✅ [TEST 4] CSV analysis: PASS

**如果失败**:
1. 检查后端窗口的错误日志
2. 确认端口 5000 未被占用
3. 验证依赖包已安装

---

## 🌐 第三步：启动前端

### 使用自动脚本（推荐）

```powershell
cd C:\Users\14593\CascadeProjects\circular-bias-detection\check-sleuth-ai
.\Run-Manual.ps1
```

或者双击 `check-sleuth-ai` 文件夹中的 `Run-Manual.ps1`

### 手动启动

```powershell
cd C:\Users\14593\CascadeProjects\circular-bias-detection\check-sleuth-ai
npm run dev
```

**预期输出**:
```
  VITE v6.2.0  ready in XXX ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: http://192.168.x.x:3000/
```

---

## 🧪 第四步：前端功能测试

### Test 1: 页面加载

1. 打开浏览器
2. 访问: http://localhost:3000
3. **预期**: 看到深色主题的上传界面

**成功标志**:
- ✅ 页面正常加载
- ✅ "Check Sleuth AI" 标题显示
- ✅ CSV 上传区域可见

---

### Test 2: CSV 上传与分析

#### 使用测试文件

文件位置: `check-sleuth-ai\test_sample.csv`

1. 点击上传区域
2. 选择 `test_sample.csv`
3. 等待 1-3 秒

**预期结果**:
- ✅ 文件名显示: "Analysis for: test_sample.csv"
- ✅ 数据表格显示（10 行 × 8 列）
- ✅ AI 分析结果显示

**分析结果应包含**:
- Summary（摘要）
- Data Quality Insights（数据质量洞察）
- Bias Detection Insights（偏差检测洞察）

---

### Test 3: Demo Mode 验证

**如果没有配置 GEMINI_API_KEY**:
- ✅ 应显示 "Demo Mode - Sample Analysis" 提示
- ✅ 显示蓝色渐变背景的提示框
- ✅ "Get Free API Key" 按钮可见
- ✅ 底部显示 "Ready for Real AI Insights?" CTA

**如果配置了 API Key**:
- ✅ 无 Demo Mode 提示
- ✅ 显示真实的 AI 分析结果
- ✅ 分析时间 1-3 秒

---

### Test 4: 重新上传

1. 点击 "Analyze Another CSV" 按钮
2. 应该返回到上传界面
3. 可以上传新文件

**预期**:
- ✅ 界面重置
- ✅ 可以再次上传
- ✅ 无错误消息

---

## 🔗 第五步：端到端集成测试

### 完整流程测试

1. **前端上传 CSV**
2. **后端接收并处理**
3. **调用偏差检测（使用适配器）**
4. **返回结果到前端**
5. **前端展示分析结果**

### 验证点

**浏览器 Network 标签**:
```
POST /api/analyze-csv
Status: 200 OK
Response time: < 3s
```

**后端日志**:
```
[OK] circular-bias-detector package loaded successfully
[Computing] PSI...
[ERROR] Package implementation failed: ...
[FALLBACK] Attempting to use legacy core implementation...
[Computing] PSI...
[Computing] CCS...
[Computing] rho_PC...
```

**预期行为**:
- ✅ 包尝试使用但失败
- ✅ 自动回退到 core 实现
- ✅ 功能正常工作
- ✅ 用户体验无影响

---

## 📊 测试矩阵

| 测试项 | 测试内容 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| **后端** |
| 1.1 | 服务器启动 | Port 5000 运行 | [ ] |
| 1.2 | 包加载 | [OK] 消息显示 | [ ] |
| 1.3 | Health check | 200 OK | [ ] |
| 1.4 | API info | 返回端点列表 | [ ] |
| 1.5 | 偏差检测 | 返回 CBS 分数 | [ ] |
| 1.6 | CSV 分析 | 返回 AI 分析 | [ ] |
| **前端** |
| 2.1 | 页面加载 | 3000 端口可访问 | [ ] |
| 2.2 | CSV 上传 | 文件选择成功 | [ ] |
| 2.3 | 数据显示 | 表格渲染正确 | [ ] |
| 2.4 | 分析结果 | 三个模块显示 | [ ] |
| 2.5 | Demo Mode | 提示正确显示 | [ ] |
| 2.6 | 重新上传 | 功能正常 | [ ] |
| **集成** |
| 3.1 | 端到端流程 | 完整无错误 | [ ] |
| 3.2 | 错误处理 | 友好提示 | [ ] |
| 3.3 | 性能 | 响应 < 3s | [ ] |
| 3.4 | 稳定性 | 多次测试稳定 | [ ] |

---

## 🐛 常见问题排查

### 问题 1: 后端启动失败

**症状**: 端口占用或导入错误

**解决**:
```powershell
# 检查端口
netstat -ano | findstr :5000

# 终止占用进程
taskkill /PID <PID> /F

# 重新安装依赖
pip install -r requirements.txt
```

---

### 问题 2: 前端无法连接后端

**症状**: Network Error 或 CORS 错误

**检查清单**:
- [ ] 后端是否在运行？(`http://localhost:5000/health`)
- [ ] Vite 代理配置是否正确？(`vite.config.ts`)
- [ ] 防火墙是否阻止？

---

### 问题 3: 适配器回退

**症状**: 看到 `[FALLBACK]` 消息

**说明**: 这是**正常行为**！

- ✅ 包尝试但数据格式不兼容
- ✅ 自动回退到原始实现
- ✅ 功能完全正常

**不需要修复**，系统设计如此。

---

### 问题 4: Unicode 编码错误

**症状**: `'gbk' codec can't encode character`

**已修复**: 所有表情符号已替换为文本

如果仍出现:
```powershell
# 设置环境变量
$env:PYTHONIOENCODING="utf-8"
```

---

## ✅ 测试通过标准

### 基本功能（必须）

- [x] 后端服务器正常启动
- [x] Health check 返回 200 OK
- [x] 前端页面正常加载
- [x] CSV 文件可以上传
- [x] 分析结果正确显示

### 高级功能（可选）

- [ ] Demo Mode 提示正确显示
- [ ] CTA 按钮可点击
- [ ] Google Analytics 初始化（如已配置）
- [ ] 错误处理友好
- [ ] 多次上传稳定

---

## 📈 性能基准

| 指标 | 目标 | 实际 |
|------|------|------|
| 后端启动时间 | < 5s | ___ |
| Health check | < 100ms | ___ |
| 偏差检测 | < 2s | ___ |
| CSV 分析 (Mock) | < 500ms | ___ |
| CSV 分析 (Real AI) | < 3s | ___ |
| 前端加载时间 | < 2s | ___ |
| 完整流程 | < 5s | ___ |

---

## 🎉 测试完成检查清单

完成所有测试后，确认：

- [ ] 所有后端 API 端点工作正常
- [ ] 前端可以上传和显示 CSV
- [ ] 偏差检测功能正常
- [ ] AI 分析功能正常（Demo 或真实）
- [ ] 错误处理友好
- [ ] 性能符合预期
- [ ] 无严重 Bug
- [ ] 日志输出清晰
- [ ] 代码整合成功
- [ ] 系统生产就绪

---

## 📞 需要帮助？

如果测试中遇到问题：

1. **查看日志**: 后端窗口的输出
2. **浏览器控制台**: 检查前端错误
3. **网络标签**: 查看 API 请求
4. **重启服务**: 尝试重新启动前后端

**文档参考**:
- `INTEGRATION_COMPLETE.md` - 整合详情
- `MANUAL_SETUP.md` - 手动设置指南
- `DEPLOYMENT_GUIDE.md` - 部署指南

---

## 🚀 测试通过后

如果所有测试通过，你可以：

1. **部署到生产环境** (Vercel/Netlify)
2. **配置 Google Analytics** (如需要)
3. **分享给用户测试**
4. **收集反馈并优化**

**恭喜！系统整合成功并通过测试！** 🎉

---

**测试日期**: ___________  
**测试人员**: ___________  
**测试结果**: [ ] 通过  [ ] 部分通过  [ ] 失败  
**备注**: _____________________________________
